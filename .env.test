# Test Environment Configuration

# Database
MONGO_URI_TEST=mongodb://localhost:27017/holy-family-test

# JWT Secret (Use different key for testing)
JWT_SECRET=test_secret_key_for_testing_only_do_not_use_in_production

# Server Configuration
PORT=5001
NODE_ENV=test

# Base URL for E2E tests
BASE_URL=http://localhost:3000

# Test User Credentials (for E2E tests)
TEST_USER_EMAIL=testuser@example.com
TEST_USER_PASSWORD=Test@1234
TEST_ADMIN_EMAIL=admin@example.com
TEST_ADMIN_PASSWORD=Admin@1234

# Email Configuration (Mock for tests)
EMAIL_SERVICE=mock
EMAIL_HOST=smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_USER=test
EMAIL_PASS=test

# Google OAuth (Mock for tests)
GOOGLE_CLIENT_ID=test_client_id
GOOGLE_CLIENT_SECRET=test_client_secret

# Razorpay (Test keys)
RAZORPAY_KEY_ID=test_key_id
RAZORPAY_KEY_SECRET=test_key_secret

# API Rate Limiting (Relaxed for tests)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000
